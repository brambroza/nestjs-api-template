/** Test doubles for sibling sub-module specs (not part of the runtime surface). */
export {
  FakeNumbers,
  FakeTx,
  FixedClock,
  InMemoryPricing,
  InMemorySalesRefLookup,
  tenantOf,
} from './application/testing/in-memory';
