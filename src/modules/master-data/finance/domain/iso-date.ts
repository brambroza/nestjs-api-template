/**
 * IsoDate moved to the shared kernel in Phase B (sales/purchase
 * documents are date-valued too). Re-exported here so finance code and
 * its tests keep their import paths.
 */
export * from '../../../../shared/domain/iso-date';
